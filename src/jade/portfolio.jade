.portfolio-p.content-block-full
    .container
        .row.page-controls
            .col logo
            .col.text-right
                button(onclick="toggleModal('.portfolio-p')")
                    i.fa.fa-arrow-left(aria-hidden='true')
        .row
            .col
                p You will need to have control over the font that is used in order to use this text. You will need to have control over the font that is used in order to use this text. You will need to have control over the font that is used in order to use this text. You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.
                p You will need to have control over the font that is used in order to use this text.